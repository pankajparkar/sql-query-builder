import { ListenToRouteDirective } from './listen-to-route.directive';

describe('ListenToRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenToRouteDirective();
    expect(directive).toBeTruthy();
  });
});
